[tools]
rust = "latest"
trunk = "latest"


[tasks]
fmt = "trunk fmt --all"
check = "trunk check --all"
repomix = "bunx repomix --copy"

check_green = "cargo check && cargo build && mise run fmt && mise run check"
